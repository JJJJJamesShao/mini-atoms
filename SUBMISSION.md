# mini-atoms 补充说明

## 1. 实现思路

mini-atoms 是一个 **AI Agent 驱动** 的应用生成系统，核心设计围绕"如何让 LLM 可靠地生成可运行的代码"展开。

### 关键设计决策

**多阶段 Agent 流水线**

将代码生成拆解为 4 个角色协作的流水线：PM（需求澄清）→ Architect（规格生成）→ Engineer（代码生成）→ Reviewer（自动校验）。每个角色有独立的 Memory 池，通过 Topic-based 消息系统共享上下文，避免单轮 Prompt 过载。

**Search/Replace Patch 修复**

发现 LLM 完整重写代码时 token 浪费严重且易引入新问题。引入 Aider 风格的 Patch 工具：校验失败后，LLM 输出精确的 SEARCH/REPLACE 指令，后端执行字符串替换。修复轮次从 2-3 轮降至 1-2 轮，token 消耗降低 90%+。

**对话式迭代**

传统生成器每次从头开始，无法基于现有代码修改。我们让 `sendFollowUp` 将当前版本 HTML 传入后端，LLM 基于现有代码做增量 Patch 修改，追加版本到同一项目。用户可以说"把背景改成蓝色"，只改 CSS，保留所有游戏逻辑。

**单文件 HTML 架构（当前阶段）**

`iframe srcDoc` 无 base URL，无法加载相对路径资源。LLM 输出结构化 JSON（CodeArtifact），后端 `mergeToSingleHtml` 自动内联 CSS/JS 为单文件。未来接入 WebContainers 后可升级为多文件项目。

**多 Provider 降级**

GLM-5.2 为主力（128K 上下文 + 流式），百炼 Qwen 为降级路径。Generate 节点失败时自动切换，避免单点故障。

---

## 2. 当前完成度

### 核心功能 ✅

| 功能                | 状态 | 说明                                       |
| ------------------- | ---- | ------------------------------------------ |
| 多阶段 Agent 流水线 | ✅   | 4 角色协作，Memory 隔离，EventBus 实时推送 |
| 自然语言生成应用    | ✅   | 支持游戏/工具类应用生成                    |
| 自动代码校验        | ✅   | 语法 + 安全 + 结构三级校验                 |
| Patch 修复          | ✅   | Search/Replace 精确编辑，最多 5 轮         |
| 对话式迭代          | ✅   | 基于现有代码增量修改                       |
| 版本管理            | ✅   | 多版本切换、项目置顶/删除                  |
| 沙箱预览            | ✅   | iframe 渲染 + 加载缓冲                     |
| 一键导出            | ✅   | 下载 HTML 文件                             |
| 代码质量评分        | ✅   | 版本完成后显示质量分数                     |
| 用户系统            | ✅   | 注册/登录/角色权限                         |

### 测试覆盖 ✅

67 个测试全部通过：SOP 引擎、架构隔离、代码校验、数据库操作。

### 已知限制 ⚠️

- **单文件架构**：复杂项目（如坦克大战）可能超出单文件承载能力
- **代码上限**：LLM 单次生成长度约 5000-8000 行有效代码
- **无实时协作**：当前为单人使用

---

## 3. AI Coding 工具使用说明

### 使用的工具

| 工具                   | 用途                                           | 占比 |
| ---------------------- | ---------------------------------------------- | ---- |
| **Kimi (Moonshot AI)** | 主力编码助手，负责架构设计、代码实现、Bug 修复 | ~70% |
| **GitHub Copilot**     | IDE 内代码补全，辅助日常编码                   | ~20% |
| **Claude / ChatGPT**   | 复杂逻辑推理、方案对比、文档撰写               | ~10% |

### AI 如何参与开发

**架构设计阶段**

- 与 Kimi 讨论系统架构：Agent 流水线设计、Memory 隔离方案、SOP 路由策略
- AI 生成架构图和流程图（Mermaid）

**编码阶段**

- Kimi 生成核心模块代码（engine.ts、llm-executors.ts、patch.ts）
- 人工 Review 后调整，AI 根据反馈迭代
- 测试用例由 AI 生成，人工补充边界场景

**调试阶段**

- 遇到 Bug 时，将错误信息和相关代码片段提供给 AI
- AI 分析根因并给出修复方案（如 reasoning_content 混入问题）
- 人工验证修复是否引入新问题

**文档阶段**

- AI 辅助撰写 README、CHANGELOG、设计文档
- 人工调整结构和重点

### 关键经验

1. **AI 适合生成框架代码，不适合微调细节**：Patch 工具的核心逻辑由 AI 设计，但边界情况（如嵌套 markdown 代码块）需要人工处理
2. **Prompt 工程比模型选择更重要**：同样的 GLM-5.2，好的 Prompt（精确错误定位 + 代码片段）能让修复成功率从 30% 提升到 80%
3. **AI 会犯错，需要校验层**：必须构建自动校验（syntax/security/structure），不能信任 LLM 的"自我检查"

---

## 4. 继续投入的扩展方向

### 短期（1-2 周）

| 方向                   | 说明                                                                           |
| ---------------------- | ------------------------------------------------------------------------------ |
| **WebContainers 集成** | 接入 StackBlitz WebContainers，突破单文件限制，支持多文件项目 + npm 依赖       |
| **代码覆盖率测试**     | verify 阶段注入测试用例（如"点击按钮是否触发事件"），用 Puppeteer 在服务端运行 |
| **用户反馈循环**       | 用户点击"这个不对"自动触发修复，无需重新输入需求                               |

### 中期（1-2 月）

| 方向                | 说明                                                                                         |
| ------------------- | -------------------------------------------------------------------------------------------- |
| **多阶段分层生成**  | 复杂项目按层次分阶段生成：底层工具 → 物理引擎 → 实体层 → 渲染/UI，每阶段验证后再进入下一阶段 |
| **专用 Apply 模型** | 像 Cursor 那样训练专门做代码变更应用的模型，替代通用的 SEARCH/REPLACE Patch                  |
| **实时协作**        | 多人同时编辑同一代码，类似 Figma 的多人协作                                                  |

### 长期（3-6 月）

| 方向               | 说明                                                                                                          |
| ------------------ | ------------------------------------------------------------------------------------------------------------- |
| **atoms 生态对接** | 与 MetaGPT / atoms 框架深度集成，支持更复杂的软件工程流程（设计文档 → 架构评审 → 代码实现 → 测试覆盖 → 部署） |
| **私有化部署**     | 支持企业内网部署，对接企业内部 LLM（如通义千问、文心一言）                                                    |
| **自定义 Agent**   | 用户可自定义 Agent 角色和 SOP 流程，适配特定业务场景                                                          |

---

**核心信念**：AI 不是替代工程师，而是让工程师专注于更高层次的架构设计和创意实现。mini-atoms 的目标是成为"AI 时代的 IDE"——不是写代码的工具，而是思考软件的工具。
